<div class="fields">
  <div class="name">
    <h4>Nome</h4>
    <input type="text" name="name" value="{{chef.name}}" required>
  </div>

  <div class="avatar_url">
    <h4>Avatar</h4>
    <input type="url" name="avatar_url" value="{{chef.avatar_url}}" required>
  </div>
</div>