{% extends 'layouts/users.njk' %}

{% block head %}
  <title>Foodfy - Início</title>
{% endblock %}

{% block content %}
  <section class="hero">
    <div class="hero-content">
      <h1>As melhores receitas</h1>
      <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
    </div>

    <img src="./assets/chef.png" alt="Imagem do Chef de Cozinha">
  </section>

  <main>
    <h2>Mais recentes</h2>
    <div class="cards">
      {% for recipe in recipes %}
        <div class="card">
          <a href="/recipes/{{recipe.id}}">
            <img src="{{recipe.file.src}}" alt="Imagem de {{recipe.chef_name}}">
            <h4>{{recipe.title}}</h4>
            <p>por {{recipe.chef_name}}</p>
          </a>
        </div>
      {% endfor %}
    </div>
  </main>
{% endblock %}